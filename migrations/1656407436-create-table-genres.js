exports.up = async (sql) => {
  await sql`CREATE TABLE genres (
  id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  genre_name varchar(50) NOT NULL
  )`;
};

exports.down = async (sql) => {
  await sql`DROP TABLE genres`;
};
