exports.up = async (sql) => {
  await sql`CREATE TABLE genders (
  id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  gender_name varchar(50) NOT NULL
  )`;
};

exports.down = async (sql) => {
  await sql`DROP TABLE genders`;
};
